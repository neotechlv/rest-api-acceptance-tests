# Sample REST API acceptance tests

REST clients are generated on the fly from Docker Compose

Run tests by `./gradlew clean downloadAllSwaggerJsons test`

Generate Allure report:  `./gradlew allureReport`